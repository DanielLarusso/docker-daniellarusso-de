<VirtualHost *:80>
    ServerName ${HOSTNAME}
    ServerAlias www.${HOSTNAME}

    DocumentRoot /var/www/html/public

    <Directory "/var/www/html/public">
        AllowOverride all
        Order Allow,Deny
        Allow from All
    </Directory>
</VirtualHost>
